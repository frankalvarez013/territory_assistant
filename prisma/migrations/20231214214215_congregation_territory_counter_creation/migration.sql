-- CreateTable
CREATE TABLE "CongregationTerritoryCounter" (
    "congregationID" TEXT NOT NULL,
    "nextTerritoryID" INTEGER NOT NULL,

    CONSTRAINT "CongregationTerritoryCounter_pkey" PRIMARY KEY ("congregationID")
);
